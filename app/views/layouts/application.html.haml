!!!
%html
  %head
    %title= content_for?(:title) ? "#{content_for(:title)} | Longbox" : 'Longbox'
    =# stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body

    %header
      %h1 Longbox

    %nav
      %ul
        %li= link_to 'View All Books', books_path
        %li= link_to 'View All Jobs', jobs_path
        %li= link_to 'View All People', people_path
        %li= link_to 'View All Series', series_index_path

    = yield

    %footer
      %p &nbsp;