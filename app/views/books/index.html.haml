%h2 All Books

= link_to 'New book', books_new_path

- unless @books.empty?
  %table
    %thead
      %tr
        %th &nbsp;
        %th Book
        %th Publisher
    %tbody
      - @books.each do |book|
        %tr
          %td= link_to '[view]', book
          %td= book.name
          %td= book.publisher_name

= link_to 'New book', books_new_path