query Creator($id: ID!) {
  creator(id: $id) {
    id
    firstName
    lastName
    createdAt
    updatedAt
    credits {
      id
      role
      book {
        id
        displayName
      }
    }
  }
}
