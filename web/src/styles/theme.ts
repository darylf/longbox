// Sizes for typography scale
const typography = {
  xs: "0.64rem",
  sm: "0.8rem",
  base: "1rem",
  lg: "1.25rem",
  xl: "1.563rem",
  xxl: "1.953rem",
};

export const theme = {
  colors: {},
  fontSizes: typography,
};

export type ThemeType = typeof theme;
