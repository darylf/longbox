query Series($id: ID!) {
  series(id: $id) {
    id
    name
    books {
      id
      displayName
      format
      issue
      price
      pageCount
      publicationDate
    }
    createdAt
    logo {
      url
    }
    publisherName
    updatedAt
  }
}
