setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    api: api/Dockerfile
    web: web/Dockerfile
  config:
    RAILS_ENV: development

release:
  image: api
  command:
    - rails db:schema:load db:seed

run:
  api: rails server -b 0.0.0.0
  web: yarn run start
